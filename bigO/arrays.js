// Use array when you need order

// insertion (depends) - pushing is quick, unshift is slow
// removal (depends) (see insertion)
// searching O(n)
// access O(1)

// push and pop O(1)
// sort O(n*log n)
// rest O(n)

